[build]
  publish = "."
  command = ""

[build.ignore]
  # CANCELA TODOS LOS DEPLOYS EN NETLIFY AUTOMÁTICAMENTE (CONSUMO 0 CRÉDITOS).
  # El frontend en Netlify ahora jala los datos dinámicamente desde el CDN gratuito de GitHub Pages,
  # por lo que Netlify solo actúa como un cascarón estático congelado.
  command = "exit 0"
