[build]
  publish = "netlify-proxy"
  command = ""

# Netlify solo conserva el dominio público y hace proxy a GitHub Pages.
# El sitio no se conecta al repositorio en Netlify para evitar deploys automáticos.
